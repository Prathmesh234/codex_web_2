# =============================================================================
# CodexWeb Frontend Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit your actual .env.local file to version control

# =============================================================================
# API Configuration
# =============================================================================

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Frontend URL (for OAuth callbacks)
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000

# =============================================================================
# Appwrite Configuration
# =============================================================================

# Appwrite endpoint (usually doesn't need to be changed)
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://fra.cloud.appwrite.io/v1

# Appwrite project ID (get this from your Appwrite console)
NEXT_PUBLIC_APPWRITE_PROJECT_ID=your_appwrite_project_id_here

# =============================================================================
# Development Settings
# =============================================================================

# Environment mode
NODE_ENV=development

# =============================================================================
# Optional: Analytics and Monitoring
# =============================================================================

# Add any analytics keys here if needed
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id_here

# =============================================================================
# Instructions for Frontend Setup
# =============================================================================
#
# 1. Copy this file to .env.local
# 2. Fill in your actual values
# 3. Never commit .env.local to version control
# 4. Required variables:
#    - NEXT_PUBLIC_API_URL (backend server URL)
#    - NEXT_PUBLIC_APPWRITE_PROJECT_ID (from Appwrite console)
#
# 5. Appwrite Setup:
#    - Go to https://cloud.appwrite.io/
#    - Create a new project
#    - Go to Settings > General
#    - Copy the Project ID
#    - Add to NEXT_PUBLIC_APPWRITE_PROJECT_ID
#
# 6. OAuth Setup:
#    - In Appwrite console, go to Auth > Settings
#    - Add GitHub OAuth provider
#    - Set callback URL to: http://localhost:3000/auth/success
#    - Copy GitHub Client ID and Secret from GitHub OAuth App 